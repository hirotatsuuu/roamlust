修正点
1. 国一覧で名前が無い時は一番下に行くという仕様にしていたが、データが完全にそろったので名前が無いことは無くなったので不要なコードを削除する
2. 国の詳細情報画面で基本情報の正式名称の前に国名として日本語の国名を表示する
3. 国の詳細情報画面のヘッダーは「詳細情報」としてよい（ヘッダーに国名を出す必要はない）
4. 各国のjson（restcountries/ディレクトリ内）から
"startOfWeek": "monday","maps": {
        "googleMaps": "https://goo.gl/maps/JqAnacWE2qEznKgw7",
上の情報も詳細情報に表示させる。
またmapに関してはリンクから移動できるようにする。
あと（wikipedia/ディレクトリ内のjsonの情報）article_lengthから本文が何文字かを最初に表示させる。
追加で本文の後にurlからwikipediaのリンクを張って移動できるようにする（このwikiのサイト情報ですと伝えたい）
5. 検索フォームに関して、予測を上に表示させる。挙動がおかしいときがあるので確認する。
6. アプリを再開したとき（情報はそろってるので一瞬ロードする）に、文字化けするがすぐに正常に戻る。一瞬文字化けするのを防ぎたい。
7. テーマ変更時の通知の色が逆になっている
8. 何らかの不具合でアプリ使用時にデータがなくなったときにハンドリングをして最初のダウンロードページに行くようになっている？
9. ダウンロードページにも上にアプリタイトルを表示させたい

assets/ディレクトリ内のjsonファイルはすべて完璧になった。以上の修正をしたい。
プログラミング初心者でもわかりやすく丁寧に書いてほしい。すべての処理にコメントアウトで丁寧に説明をしてほしい。



絶対情報はすべてconfigに書き出してほしい。
文字をべた書きしないようにしてほしい。
コメントアウトでどのファイルのどこで使用しているのか？を丁寧に説明をして。
問い合わせフォームに関して、メールアドレスに送る方式にしたい。url_launcher？
メールアドレスももちろんconfigで設定する。



アプリ起動時、Googleフォントがインターネットからダウンロードされるまでの間に一瞬発生する？？？？？ネット通信？？？インターネットから帆取得している？オフラインだと文字化けする？

アプリの初期起動時にデータを取得するときにコンソールに「Wikiなし」とか出る国があるがこれは取得ができていない？ただ詳細情報でwikiにアクセスできているが？取得できているのにコンソール出力してしまっている？

検索フォーム入力時の予測が上に来るのは良いです。ただ例えば「ジンバブエ」と入力すると5行分縦幅を取ってしまっている。「ア」とか入力するとしっかり候補が出ているので良い。対象候補が1つの時の挙動もしっかりしたい。

全体を通して、jsonのファイルを整理したい。なのでjsonファイルの使っている項目を知りたい。wikipedaiディレクトリ内のファイルの情報はすべて使っていると思う。countries_master.jsonはiso3の情報は必要ないと思うので、使っている処理はiso2を使うようにしたい。restcountris/ディレクトリ内のファイルはどの項目を使っている？使っている情報のID一覧と逆に使っていない情報のID一覧も教えてほしい。jsonファイルを軽量にする予定です。

修正箇所のみを丁寧に教えてください。
修正箇所にかんしての修正前と後の処理とその前後くらいのみを教えてください。
プログラミング初心者でもわかりやすく丁寧にお願いします。




1.フォントに関して、一度googleからダウンロードしたらほかの情報と同様キャッシュに保持される？他の国の情報が消えるまでは確実にオフラインでも挙動する？
またオフラインで使える軽量な日本語フォントを使えるものはある？事前にダウンロードする案も並行して考える。

2.try-catch のエラー内容をより詳細に表示するように修正することで原因を特定しやすくなります。
確認したところ、wikiなしとコンソール出力されているファイルも完璧に情報はそろっているはずです。

3.今度は候補が検索フォームに被っています。被らないように検索フォームの真上に候補が出るようにしてください。
第一候補がフォームの真上、第二候補がその上を含めて2行（検索フォームに被らないこと）、となるようにしてください。数が多くても最大5個の候補までにしてください。
今後、ソースコードに「修正」と書かないこと。処理の内容をコメントアウトで説明すること。修正の話はソースコードではなく私に丁寧に説明してください。

4.ランキングでiso3を使用しているのであれば、iso2に統一しなくてよいです。masterで今まで通り保持します。(修正無し)

5.

他に、詳細画面の名称の「(日本語)」は必要ない。
ジニ係数に関しての説明を簡単にジニ係数の横に少し薄い文字で書いて。
googlemapのリンク、wikipediaのリンクをこのアプリの説明で使っているリンクのようなイメージで。ただし、スタイルやサイズは詳細画面の体裁を崩したくないので、一文の枠に収まるように小さめに。

国の一覧画面に関して、国の一覧を表示する前に（上に）、国の総数（全250か国）のような文を薄く小さく表示して。
また、並び替えをしたときには、並び替えをした項目（人口とか）を一覧表示の前に表示して。
また、並び替えを背託したときに、下に通知で「〇〇に並び替えました」というのを表示して。










